// Entry point placeholder — services will be split into
// src/ingestion-service and src/order-service as they're built.
console.log("Stadium Copilot backend — scaffold only, no service implemented yet.");
